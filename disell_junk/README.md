# Archived research material

This directory contains the synthetic phantom benchmark, paper-specific
analysis scripts, research notebooks and local project configuration. It is
kept in Git for provenance but is not required to install or use `disell` for
segmentation.

The standalone phantom package is maintained separately at
`../disell-phantoms`.
